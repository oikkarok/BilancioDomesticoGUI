import React from 'react';
import { Button } from '@mui/material';

const GestioneUtenti = () => {
  return (
    <div>
      <Button>Visualizza tutti</Button>
      <Button>Seleziona utente</Button>
    </div>
  );
};

export default GestioneUtenti;
