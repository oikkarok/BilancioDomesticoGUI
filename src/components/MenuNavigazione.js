import React from 'react';
import { Button } from '@mui/material';

const MenuNavigazione = () => {
  return (
    <div>
      <Button>Menu Utenti</Button>
      <Button>Menu Bilancio</Button>
    </div>
  );
};

export default MenuNavigazione;
